A small command line utitity to dither an image using Floyd-Steinberg dithering.
Supports both colour and grey images.

Compiled using `cargo build -r`

#Usage
dither [FILEPATH_INPUT] [FILEPATH_OUTPUT] [OPTIONS...]
